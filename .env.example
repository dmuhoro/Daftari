VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Error tracking (optional — analytics still work without it)
VITE_SENTRY_DSN=

# Referral & domain
VITE_CANONICAL_DOMAIN=https://daftari.app

# Web Push (VAPID public key — generate with: npx web-push generate-vapid-keys)
VITE_VAPID_PUBLIC_KEY=

# Admin dashboard access (your Supabase user ID)
VITE_ADMIN_USER_ID=
